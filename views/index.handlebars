<div class="jumbotron">
    <h1 class="text-center">Eat-Da-Burger!</h1>
</div>

<div class="container">
    <div class="row">
        <h2>Burger Wish List: </h2>

        <ul>
            {{#each burgers}}
                <li>
                    {{burger_name}}
                    <button class="devoured" data-id="{{id}}">Devour</button>
                </li>
            {{/each}}
        </ul>

    </div>

    <div class="row">
        <h2>Burger's Eaten: </h2>
    </div>
</div>

